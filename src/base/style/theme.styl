@import "variables.styl"

body {
  color $text-color
  font-size $font-size-base
}

/*iconfont initialized*/
.icon {
  width: 1em;
  height: 1em;
  vertical-align: -0.15em;
  fill: currentColor;
  overflow: hidden;
}
